dfQUERY: # one record per query
  strStudyCol: protocolname
  strIDCol: subjectname
  strVisitCol: visit
  strFormCol: formoid
  strFieldCol: fieldoid
  strLogNumberCol: log_number
  strDataPointIDCol: datapointid
  strStatusCol: querystatus
  strStatusVal:
    - Open
    - Answered
    - Closed
  strCreationDateCol: created
  strAnswerDateCol: answered
  strResolutionDateCol: resolved
  strMarkingGroupCol: markinggroup
  strQueryAgeCol: queryage
dfDATAENT: # one record per data page
  strStudyCol: protocolname
  strIDCol: subjectname
  strVisitCol: visit
  strFormCol: formoid
  strVisitDateCol: visitdat_date
  strEntryDateCol: min_entereddate
  strDataEntryLagCol: data_entry_lag
dfDATACHG: # one record per data point
  strStudyCol: protocolname
  strIDCol: subjectname
  strVisitCol: visit
  strFormCol: formoid
  strFieldCol: fieldoid
  strLogNumberCol: log_number
  strDataPointIDCol: datapointid
  strNChangesCol: n_changes
  strRequiredCol: isrequired

