class: data.frame
definition: >
  Input data with one record per subject. Created by passing
  Raw+ data into [Input_Rate()]. Expected columns: `GroupID`,
  `GroupLevel`, `Numerator`, `Denominator` and/or columns specified
  in `strCountCol` and `strGroupCol`.
